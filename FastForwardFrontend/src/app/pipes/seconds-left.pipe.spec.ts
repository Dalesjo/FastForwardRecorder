import { SecondsLeftPipe } from './seconds-left.pipe';

describe('SecondsLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
